enum OptionsError {
    case invalidOption(option: string)
    case unknownOption(option: string)
    case noOutputOption
    case noFilesToCompile
}

enum ParserError {
    case syntax(line: number, column: number)
}

enum CompilerError {
    case parser(line: number, column: number)
}