define ListIterator[T] (current: pointer[Node[T]])
{
    function next(this: ListIterator[T]) returns ref[T]
    {
        if current <> null
        {
            var old_current current
            set current: current.next
            return (&old_current.element) as ref[T]
        }
        else
            return null
    }
}
