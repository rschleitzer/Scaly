use scaly.memory.Page

define containers {
    ; Container modules disabled for now - require language features not yet implemented:
    ; - Static array constants (HASH_PRIMES)
    ; - RBMM ^page_ptr syntax for allocating on returned page pointers
    ; - while let bindings
    ; - sizeof/alignof expressions
    ; - Various C intrinsics (memcpy, memset, sqrt, etc.)
}
