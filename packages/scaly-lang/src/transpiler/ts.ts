// Scaly TypeScript Transpiler - for bootstrapping
// Generates TypeScript code similar to retired/packages structure
// TODO: implement when parser/syntax are generated

export class TsTranspiler {
  transpile(program: unknown): string {
    // Placeholder - will be implemented when syntax types are generated
    return String(program)
  }
}
