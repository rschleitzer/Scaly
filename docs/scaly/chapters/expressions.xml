<chapter xml:id="expressions">
  <title>Expressions</title>

  <section xml:id="expr-overview">
    <title>Overview</title>
    <para>
      Expressions are the core computational units of Scaly. Every expression
      evaluates to a value. Unlike many languages, Scaly uses postfix operators
      and does not define operator precedence — evaluation order is always
      explicit.
    </para>
  </section>

  <section xml:id="expr-literals">
    <title>Literal Expressions</title>
    <para>
      The simplest expressions are literals. A literal evaluates to itself.
    </para>

    <section xml:id="expr-literals-boolean">
      <title>Boolean Literals</title>
      <para>
        The boolean literals are <literal>true</literal> and <literal>false</literal>.
      </para>
      <programlisting>
true            ; evaluates to true
false           ; evaluates to false
      </programlisting>
    </section>

    <section xml:id="expr-literals-integer">
      <title>Integer Literals</title>
      <para>
        Integer literals are sequences of decimal digits, optionally preceded
        by a minus sign for negative numbers.
      </para>
      <programlisting>
42              ; evaluates to 42
0               ; evaluates to 0
-7              ; evaluates to -7
      </programlisting>
    </section>

    <section xml:id="expr-literals-hex">
      <title>Hexadecimal Literals</title>
      <para>
        Hexadecimal literals start with <literal>0x</literal> or <literal>0X</literal>
        followed by hexadecimal digits (0-9, a-f, A-F).
      </para>
      <programlisting>
0xFF            ; evaluates to 255
0x1A            ; evaluates to 26
0x0             ; evaluates to 0
      </programlisting>
    </section>

    <section xml:id="expr-literals-float">
      <title>Floating-Point Literals</title>
      <para>
        Floating-point literals contain a decimal point separating the integer
        and fractional parts.
      </para>
      <programlisting>
3.14            ; evaluates to 3.14
2.0             ; evaluates to 2
0.5             ; evaluates to 0.5
      </programlisting>
    </section>

    <section xml:id="expr-literals-string">
      <title>String Literals</title>
      <para>
        String literals are enclosed in double quotes. They may span multiple
        lines, with line breaks preserved in the resulting string.
      </para>
      <programlisting>
"hello"         ; evaluates to "hello"
"Hello, World!" ; evaluates to "Hello, World!"
""              ; evaluates to "" (empty string)
      </programlisting>
      <para>
        Escape sequences are supported: <literal>\\</literal> (backslash),
        <literal>\"</literal> (double quote), <literal>\'</literal> (single quote),
        <literal>\n</literal> (newline), <literal>\r</literal> (carriage return),
        <literal>\t</literal> (tab), and <literal>\0</literal> (null).
      </para>
      <programlisting>
"line one
line two"       ; multiline string with embedded newline
"tab\there"     ; string with tab character
"say \"hi\""    ; string with escaped quotes
      </programlisting>
    </section>

    <section xml:id="expr-literals-character">
      <title>Character Literals</title>
      <para>
        Character literals represent a single character, enclosed in backticks.
      </para>
      <programlisting>
`a`             ; evaluates to "a"
`Z`             ; evaluates to "Z"
      </programlisting>
    </section>

    <section xml:id="expr-literals-fragment">
      <title>Fragment Literals</title>
      <para>
        Fragment literals are used within string interpolation to represent
        literal text segments between interpolated expressions.
      </para>
    </section>

  </section>

  &generated-tests;

</chapter>
