; Hello World - standalone AOT compilation test
; Usage: ./scalyc/build/scalyc -o hello hello.scaly && ./hello

function strlen(s: pointer[const_char]) returns size_t extern
function write(fd: int, buf: pointer[void], count: size_t) returns size_t extern

function main() returns int
{
    let msg "Hello, World!\n"
    write(1, msg as pointer[void], strlen(msg))
    0
}
