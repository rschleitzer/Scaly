if args.length() < 1 {
    return 1
}
else {
    let options: Options = Options.parseArguments(args)
        catch invalidOption return 2
        catch noOutputOption return 3
        catch noFilesToCompile return 4

    Compiler.compile(options) catch _ return 5
}
