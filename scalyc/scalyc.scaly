if args.length < 1 {
    throw
}
else {
    let options: CompilerOptions = CompilerOptions.ParseArguments(args)
        catch invalidOption throw
        catch noOutputOption throw
        catch noFilesToCompile throw

    Compiler.compile(options) catch _ throw
}
