if args.length() < 1 {
    return 1
}
else {
    let options: Options = Options.parseArguments(args)
        catch OptionsError.invalidOption() return 2
        catch OptionsError.noOutputOption() return 3
        catch OptionsError.noFilesToCompile() return 4

    Compiler.compile(options) catch _ return 5
}
