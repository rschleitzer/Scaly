if args.length < 1 {
    throw 1
}
else {
    let options: CompilerOptions = CompilerOptions.ParseArguments(args)
        catch invalidOption throw 2
        catch noOutputOption throw 3
        catch noFilesToCompile throw 4

    Compiler.compile(options) catch _ throw 5
}
