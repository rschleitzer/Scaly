; AOT Test: Function with arguments
; Expected: 15

function add(a: int, b: int) returns int
    a + b

function multiply(x: int, y: int) returns int
    x * y

let sum add(3, 4)
let product multiply(sum, 2)
if product = 14
    print("15")
else
    print("WRONG")
